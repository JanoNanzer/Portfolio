import { createTheme } from '@mui/material/styles';

const coralColor = createTheme({
    palette: {
      primary: {
        main: '#ff7f50',
      },
      secondary: {
        main: '#34495e',
      },
    },
});

export default coralColor